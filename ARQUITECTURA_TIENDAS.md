# 🏗️ Arquitectura del Sistema de Tiendas - Lyrium

## 📊 Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────────┐
│                        LYRIUM MARKETPLACE                            │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
          ┌─────────▼─────────┐       ┌────────▼────────┐
          │   PANEL ADMIN     │       │  REGISTRO       │
          │   (Lyrium)        │       │  PÚBLICO        │
          └─────────┬─────────┘       └────────┬────────┘
                    │                          │
                    │ Gestiona                 │ Crea
                    │                          │
          ┌─────────▼──────────────────────────▼─────────┐
          │           TABLA: tiendas                      │
          │  ┌──────────────────────────────────────┐    │
          │  │ id, email, password, nombre_tienda,  │    │
          │  │ plan, estado, estadísticas, etc.     │    │
          │  └──────────────────────────────────────┘    │
          └─────────┬──────────────────────────┬─────────┘
                    │                          │
        ┌───────────┴──────────┐    ┌──────────┴──────────┐
        │                      │    │                     │
┌───────▼────────┐   ┌─────────▼────▼──────┐   ┌─────────▼─────────┐
│ TIENDA LOGIN   │   │   PANEL TIENDA      │   │  VISTA PÚBLICA    │
│ (Auth)         │──▶│   (Dashboard)       │   │  (tienda.php)     │
└────────────────┘   └─────────┬───────────┘   └───────────────────┘
                               │
                ┌──────────────┼──────────────┐
                │              │              │
        ┌───────▼──────┐ ┌────▼─────┐ ┌──────▼──────┐
        │  PRODUCTOS   │ │ PEDIDOS  │ │ CONFIGURAC. │
        │  (CRUD)      │ │ (Gestión)│ │ (Settings)  │
        └──────────────┘ └──────────┘ └─────────────┘
```

## 🔄 Flujo de Datos

```
┌──────────────────────────────────────────────────────────────────┐
│                    FLUJO DE REGISTRO                              │
└──────────────────────────────────────────────────────────────────┘

1. CLIENTE/VENDEDOR
   └─▶ /registrar-tienda/index.php
       ├─ Llena formulario
       ├─ Email, password, nombre tienda, plan
       └─▶ POST /backend/panel-tienda/api/auth.php?action=register

2. BACKEND
   └─▶ Valida datos
       ├─ Hash de password
       ├─ Genera slug único
       ├─ INSERT INTO tiendas (estado='pendiente')
       └─▶ Email de confirmación

3. ADMIN
   └─▶ /backend/admin/tiendas/listar.php
       ├─ Ve tienda pendiente
       ├─ Revisa información
       └─▶ Aprueba (estado='activo')

4. TIENDA
   └─▶ Recibe email de activación
       └─▶ Puede acceder a /panel-tienda/login.php

┌──────────────────────────────────────────────────────────────────┐
│                    FLUJO DE AUTENTICACIÓN                         │
└──────────────────────────────────────────────────────────────────┘

1. TIENDA
   └─▶ /panel-tienda/login.php
       ├─ Email: tienda@ejemplo.com
       ├─ Password: ********
       └─▶ POST /backend/panel-tienda/api/auth.php?action=login

2. BACKEND
   └─▶ Valida credenciales
       ├─ SELECT * FROM tiendas WHERE email=? AND estado='activo'
       ├─ password_verify()
       ├─ Crea sesión
       │  ├─ $_SESSION['tienda_id']
       │  ├─ $_SESSION['tienda_nombre']
       │  ├─ $_SESSION['tienda_plan']
       │  └─ $_SESSION['tienda_slug']
       └─▶ Redirect a /panel-tienda/index.php

3. MIDDLEWARE
   └─▶ En cada página del panel
       ├─ include 'middleware/auth_tienda.php'
       ├─ Verifica $_SESSION['tienda_id']
       └─▶ Si no existe → Redirect a login

┌──────────────────────────────────────────────────────────────────┐
│                    FLUJO DE PRODUCTOS                             │
└──────────────────────────────────────────────────────────────────┘

1. CREAR PRODUCTO
   └─▶ /panel-tienda/productos/crear.php
       ├─ Formulario de producto
       ├─ Verifica límite del plan
       │  └─ Plan Básico: max 50 productos
       └─▶ POST /backend/panel-tienda/api/productos.php

2. BACKEND
   └─▶ Valida datos
       ├─ Verifica límite: COUNT(*) FROM productos WHERE tienda_id=?
       ├─ Sube imágenes (max según plan)
       ├─ INSERT INTO productos (tienda_id=?, ...)
       └─▶ Trigger actualiza tiendas.total_productos

3. VISTA PÚBLICA
   └─▶ /tienda.php?slug=vida-natural
       ├─ SELECT * FROM productos WHERE tienda_id=? AND estado=1
       └─▶ Muestra productos de la tienda

┌──────────────────────────────────────────────────────────────────┐
│                    FLUJO DE PEDIDOS                               │
└──────────────────────────────────────────────────────────────────┘

1. CLIENTE
   └─▶ Realiza compra en tienda pública
       └─▶ INSERT INTO pedidos (tienda_id=?, cliente_id=?, ...)

2. TRIGGER
   └─▶ Actualiza estadísticas
       ├─ UPDATE tiendas SET total_pedidos++, total_ventas+=total
       └─▶ Notificación a tienda (email/push)

3. TIENDA
   └─▶ /panel-tienda/pedidos/index.php
       ├─ SELECT * FROM pedidos WHERE tienda_id=?
       ├─ Ve nuevo pedido
       └─▶ Cambia estado: pendiente → en_proceso → enviado → entregado
```

## 🗄️ Relaciones de Base de Datos

```
┌─────────────────┐
│    tiendas      │
│  PK: id         │
│  UK: email      │
│  UK: slug       │
└────────┬────────┘
         │
         │ 1:N
         │
    ┌────┴────────────────────────────────────┐
    │                                         │
┌───▼──────────────┐              ┌───────────▼─────────┐
│   productos      │              │     pedidos         │
│  PK: id          │              │  PK: id             │
│  FK: tienda_id   │              │  FK: tienda_id      │
└──────────────────┘              │  FK: cliente_id     │
                                  └─────────────────────┘
         │
         │ 1:N
         │
┌────────▼──────────────────────────────────────────────┐
│                                                        │
│  tiendas_categorias                                    │
│  tiendas_cuentas_bancarias                            │
│  tiendas_horarios                                      │
│  tiendas_galeria                                       │
│  tiendas_sucursales                                    │
│  tiendas_tokens                                        │
│                                                        │
└────────────────────────────────────────────────────────┘
```

## 🔐 Seguridad y Permisos

```
┌─────────────────────────────────────────────────────────┐
│              MATRIZ DE PERMISOS                          │
└─────────────────────────────────────────────────────────┘

┌──────────────┬──────────┬──────────┬──────────┐
│   Acción     │  Admin   │  Tienda  │ Cliente  │
├──────────────┼──────────┼──────────┼──────────┤
│ Ver todas    │    ✅    │    ❌    │    ❌    │
│ las tiendas  │          │          │          │
├──────────────┼──────────┼──────────┼──────────┤
│ Crear tienda │    ✅    │    ❌    │    ✅*   │
│              │          │          │ (registro)│
├──────────────┼──────────┼──────────┼──────────┤
│ Editar       │    ✅    │    ✅**  │    ❌    │
│ tienda       │  (todas) │  (suya)  │          │
├──────────────┼──────────┼──────────┼──────────┤
│ Aprobar/     │    ✅    │    ❌    │    ❌    │
│ Rechazar     │          │          │          │
├──────────────┼──────────┼──────────┼──────────┤
│ Asignar plan │    ✅    │    ❌    │    ❌    │
├──────────────┼──────────┼──────────┼──────────┤
│ Ver productos│    ✅    │    ✅**  │    ✅*** │
│              │  (todos) │  (suyos) │ (públicos)│
├──────────────┼──────────┼──────────┼──────────┤
│ Crear        │    ❌    │    ✅    │    ❌    │
│ producto     │          │          │          │
├──────────────┼──────────┼──────────┼──────────┤
│ Ver pedidos  │    ✅    │    ✅**  │    ✅*** │
│              │  (todos) │  (suyos) │  (suyos) │
└──────────────┴──────────┴──────────┴──────────┘

* Registro público, queda pendiente de aprobación
** Solo de su propia tienda
*** Solo pedidos donde es cliente
```

## 📱 Endpoints de API

```
┌─────────────────────────────────────────────────────────┐
│              API PANEL TIENDA                            │
└─────────────────────────────────────────────────────────┘

AUTENTICACIÓN
├─ POST   /backend/panel-tienda/api/auth.php?action=register
├─ POST   /backend/panel-tienda/api/auth.php?action=login
├─ POST   /backend/panel-tienda/api/auth.php?action=logout
└─ POST   /backend/panel-tienda/api/auth.php?action=recuperar

PRODUCTOS
├─ GET    /backend/panel-tienda/api/productos.php
├─ GET    /backend/panel-tienda/api/productos.php?id={id}
├─ POST   /backend/panel-tienda/api/productos.php
├─ PUT    /backend/panel-tienda/api/productos.php?id={id}
└─ DELETE /backend/panel-tienda/api/productos.php?id={id}

PEDIDOS
├─ GET    /backend/panel-tienda/api/pedidos.php
├─ GET    /backend/panel-tienda/api/pedidos.php?id={id}
└─ PUT    /backend/panel-tienda/api/pedidos.php?id={id}&action=cambiar_estado

ESTADÍSTICAS
├─ GET    /backend/panel-tienda/api/estadisticas.php?tipo=ventas
├─ GET    /backend/panel-tienda/api/estadisticas.php?tipo=productos
└─ GET    /backend/panel-tienda/api/estadisticas.php?tipo=dashboard

CONFIGURACIÓN
├─ GET    /backend/panel-tienda/api/configuracion.php
├─ PUT    /backend/panel-tienda/api/configuracion.php
├─ POST   /backend/panel-tienda/api/configuracion.php?action=subir_logo
└─ POST   /backend/panel-tienda/api/configuracion.php?action=subir_banner

CATEGORÍAS
├─ GET    /backend/panel-tienda/api/categorias.php
├─ POST   /backend/panel-tienda/api/categorias.php
├─ PUT    /backend/panel-tienda/api/categorias.php?id={id}
└─ DELETE /backend/panel-tienda/api/categorias.php?id={id}

CUENTAS BANCARIAS
├─ GET    /backend/panel-tienda/api/cuentas_bancarias.php
├─ POST   /backend/panel-tienda/api/cuentas_bancarias.php
├─ PUT    /backend/panel-tienda/api/cuentas_bancarias.php?id={id}
└─ DELETE /backend/panel-tienda/api/cuentas_bancarias.php?id={id}
```

## 🎨 Estados de Tienda

```
┌─────────────────────────────────────────────────────────┐
│              CICLO DE VIDA DE UNA TIENDA                 │
└─────────────────────────────────────────────────────────┘

    [REGISTRO]
        │
        ▼
   ┌──────────┐
   │ PENDIENTE│ ◄─── Estado inicial tras registro
   └────┬─────┘
        │
        ├─── Admin revisa
        │
    ┌───▼───┐
    │APROBAR│
    └───┬───┘
        │
        ├──────────┬──────────┐
        │          │          │
        ▼          ▼          ▼
   ┌────────┐  ┌──────┐  ┌─────────┐
   │ ACTIVO │  │RECHAZ│  │SUSPENDID│
   │        │  │ADO   │  │O        │
   └────────┘  └──────┘  └────┬────┘
        │                      │
        │                      │
        │      ┌───────────────┘
        │      │
        ▼      ▼
   [OPERANDO] [BLOQUEADO]

ESTADOS:
- borrador    : Tienda en construcción (no visible)
- pendiente   : Esperando aprobación de admin
- activo      : Operando normalmente
- suspendido  : Bloqueado temporalmente (falta de pago, etc.)
- rechazado   : No aprobado por admin
```

---

**Última actualización:** 2026-01-09  
**Versión:** 1.0
